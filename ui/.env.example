# Base URL for WordPress REST API requests.
# When deploying alongside the WordPress site on the same domain, you can keep this as "/".
# For a separate domain or subdomain, set the full origin, e.g. "https://api.example.com".
VITE_API_BASE_URL=/
